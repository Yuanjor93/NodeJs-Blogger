extends layout

block layout-content
  div.View.LogoutView
    h1.Banner Bye! #{userProfile.nickname}!
    div.Message
      div.Title
        h3 Thanks for visiting!
        h1 Take care.
      pre.Details=JSON.stringify(userProfile, null, 2)
    //- div.NavButtons
    //-   a(href="/logout")
    //-     div.NavButton Log out